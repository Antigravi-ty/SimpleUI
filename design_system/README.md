# SimpleUI Design System

Agent-friendly design system architecture:
- `tokens/`: Token schemas and generated CSS variables (SSOT).
- `schemas/`: Machine-readable component definitions (props, events, states).
- `styles/`: Modular BEM component styles consuming design tokens.
- `behaviors/`: Headless interaction handlers with lifecycle management.
